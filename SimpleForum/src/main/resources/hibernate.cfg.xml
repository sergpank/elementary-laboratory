<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <property name="hibernate.bytecode.use_reflection_optimizer">false</property>
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://45.88.78.250:3306/chatdb?useSSL=false</property>

        <property name="connection.username">chat</property>
        <property name="connection.password">chat</property>

        <!-- SQL DIALECT -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">10</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>
        <property name="format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
<!--        <property name="hbm2ddl.auto">create</property>-->

<!--        <property name="hibernate.enable_lazy_load_no_trans">true</property>-->
        <mapping class="org.elementary.forum.entity.Group"/>
        <mapping class="org.elementary.forum.entity.User"/>
        <mapping class="org.elementary.forum.entity.Topic"/>
        <mapping class="org.elementary.forum.entity.Post"/>
        <mapping class="org.elementary.forum.entity.Votes"/>

    </session-factory>

</hibernate-configuration>